import React from 'react';
import ProgramDirectorMainLayout from './ProgramDirectorMainLayout';

/**
 * Componente principal para el Director de Programa
 * Este componente sirve como punto de entrada para todas las funcionalidades
 * del director de programa
 */
const ProgramDirectorIndex: React.FC = () => {
  return <ProgramDirectorMainLayout />;
};

export default ProgramDirectorIndex;
